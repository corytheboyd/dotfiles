services:
  archlinux:
    build: archlinux
    platform: linux/arm64
    volumes:
      - ../:/chezmoi-source:ro
      - pacman-cache:/var/cache/pacman/pkg
      - yay-cache:/home/robertpaulson/.cache/yay
    environment:
      - LOG_LEVEL=DEBUG

volumes:
  pacman-cache:
  yay-cache:
