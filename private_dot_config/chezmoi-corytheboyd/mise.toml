[tasks.sync]
run = [
    "chezmoi re-add ~/.config/VSCodium/User/settings.json",
    "chezmoi re-add ~/.local/share/JetBrains/Toolbox/.settings.json",
]

[tasks.update]
depends = ["sync"]
run = ["cat banner.txt", "chezmoi update"]
